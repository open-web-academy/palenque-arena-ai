import React from "react";

export default function App() {
  return (
    <div>
      <h1>🐓 Palenque Arena</h1>
      <p>Phase 2 UI - Components ready for integration after contract deployment</p>
      <p>Connect wallet → View match → Bet MON → Boost PAL → Claim winnings</p>
    </div>
  );
}
